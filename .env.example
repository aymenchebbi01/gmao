# SQLite Database Configuration
# No host/user/password needed for SQLite. 
# The database file is created automatically as gmao.db in the root directory.

# Server Configuration
PORT=5033
NODE_ENV=development
JWT_SECRET=your_jwt_secret_key_here
